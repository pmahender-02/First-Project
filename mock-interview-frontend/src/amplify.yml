version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd mock-interview-frontend
        - npm install
    build:
      commands:
        - npx vite build
  artifacts:
    baseDirectory: mock-interview-frontend/dist
    files:
      - '**/*'
  cache:
    paths:
      - mock-interview-frontend/node_modules/**/*