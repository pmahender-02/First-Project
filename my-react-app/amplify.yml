version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci   # installs dependencies from package-lock.json
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist   # Vite outputs to dist/
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
