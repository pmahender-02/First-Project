version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd my-react-app
        - npm ci
    build:
      commands:
        - cd my-react-app
        - npm run build
  artifacts:
    baseDirectory: my-react-app/dist
    files:
      - '**/*'
  cache:
    paths:
      - my-react-app/node_modules/**/*
