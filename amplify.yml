version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist   # change to build if CRA, or .next if Next.js
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
